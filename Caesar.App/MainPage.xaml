<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="Caesar.App.MainPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">

    <ScrollView>
        <VerticalStackLayout
            Padding="15"
            Spacing="20"
            VerticalOptions="Center">
            <Image
                HeightRequest="100"
                HorizontalOptions="Center"
                Source="security.png"/>
            <Label Text="Key"/>
            <Entry Text="{Binding Key}"/>
            <Label Text="Text"/>
            <Editor
                x:Name="ResultEditor"
                AutoSize="TextChanges"
                Placeholder="Your text that you want to encrypt or decrypt"
                Text="{Binding Text}"/>
            <HorizontalStackLayout HorizontalOptions="Center"
                    VerticalOptions="Center">
                <Label Padding="10"
                        Text="Encrypt"/>
                <CheckBox IsChecked="{Binding IsCrypt}"
                        VerticalOptions="Center"/>
            </HorizontalStackLayout>
            <Button Command="{Binding RunCommand}"
                    Text="Run"/>
            <Border
                Padding="20"
                StrokeShape="RoundRectangle 10"
                StrokeThickness="2">
                <Label MaximumHeightRequest="500"
                        Text="{Binding Result}"/>
            </Border>
            <Grid ColumnDefinitions="2,0.75*,0.25*">
                <Button
                    Grid.Column="2"
                    Command="{Binding CopyCommand}"
                    Text="Copy"/>
            </Grid>
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
